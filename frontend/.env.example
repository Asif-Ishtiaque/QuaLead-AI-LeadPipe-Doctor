# URL the BROWSER uses to reach the FastAPI service. In the docker-compose
# setup the API is published on the host at :8000, so from the browser it's
# localhost, NOT the internal `api` service name.
VITE_API_BASE_URL=http://localhost:8000
